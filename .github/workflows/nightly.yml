name: Nightly

on:
  schedule:
    # Every day at 03:00 UTC
    - cron: '0 3 * * *'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  # Correctness of the ~24 heavy `#[ignore]`d script snapshots — mirrors
  # `make test-slow` (see `slow_script_test!`). Too slow for every push/PR,
  # so it runs once per night. Performance of these is tracked by the
  # benchmarks.
  test-slow:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: dtolnay/rust-toolchain@stable
    - uses: Swatinem/rust-cache@v2
    - uses: taiki-e/install-action@v2
      with:
        tool: nextest
    - name: Run slow script tests
      run: make test-slow

  # Coverage-guided crash fuzzing of the parser and interpreter (see
  # fuzz/README.md). A few minutes per target per night compounds into
  # deep coverage over time; crashing inputs are uploaded as artifacts.
  fuzz:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: dtolnay/rust-toolchain@nightly
    - uses: Swatinem/rust-cache@v2
      with:
        workspaces: |
          .
          fuzz
    - uses: taiki-e/install-action@v2
      with:
        tool: cargo-fuzz
    - name: Seed corpora from the test scripts
      run: make fuzz-corpus
    - name: Fuzz the parser
      run: cargo fuzz run parse -- -timeout=10 -max_len=4096 -max_total_time=300
    - name: Fuzz the interpreter
      # Runs even when the parse target already found a crash, so one
      # night reports both.
      if: '!cancelled()'
      run: cargo fuzz run interpret -- -timeout=20 -max_len=2048 -max_total_time=300
    - name: Upload crashing inputs
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: fuzz-artifacts
        path: fuzz/artifacts/
