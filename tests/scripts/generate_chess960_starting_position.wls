(* The task picks a uniformly random valid Chess960 starting position with
   `RandomChoice` over all arrangements where the bishops are on opposite
   colours and the king sits between the two rooks. Hardcoded to one valid
   arrangement here (written with the usual K/Q/R/B/N piece letters) so the
   snapshot is deterministic. *)
Print["RNBQKBNR"]
