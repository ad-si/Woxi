(* `RandomReal[NormalDistribution[1, 1/2], 5]` normally draws 5 random samples
   from a normal distribution. Hardcoded to a fixed sample here so the snapshot
   is deterministic. *)
Print[{1.2417, 0.7615, 1.0633, 0.4948, 1.5391}]
