# `BinLists`

Group data points into equal-width bins.

```scrut
$ wo 'BinLists[x, {0, 10, 1}]'

BinLists::vectmat: The first argument is expected to be a unit-compatible vector or a matrix with unit-compatible columns.
BinLists[x, {0, 10, 1}]
```
