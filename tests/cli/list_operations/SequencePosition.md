# `SequencePosition`

Find positions of a subsequence in a list.

```scrut
$ wo 'SequencePosition[x, y]'

SequencePosition::list: List expected at position 1 in SequencePosition[x, y].
SequencePosition[x, y]
```
