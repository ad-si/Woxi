# `LinearGradientFilling`

Specifies a linear gradient color filling for graphics.

```scrut
$ wo 'LinearGradientFilling[]'
LinearGradientFilling[{0, 1} -> {GrayLevel[0], GrayLevel[1]}, 0, Fixed]
```
