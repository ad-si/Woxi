# `OptionValue`

Retrieves the value of a named option within a function using OptionsPattern.

```scrut
$ wo 'OptionValue[MySetting, bar]'

OptionValue::optnf: Option name bar not found in defaults for MySetting.
bar
```
