# `PerfectNumber`

Gives the nth perfect number.

```scrut
$ wo 'PerfectNumber[n]'

PerfectNumber::pintprm: Parameter n at position 1 in PerfectNumber[n] is expected to be a positive integer.
PerfectNumber[n]
```
