# `GeometricMean`

Returns the geometric mean of a list.

```scrut
$ wo 'GeometricMean[{2, 8}]'
4
```

On an association, the values are used.

```scrut
$ wo 'GeometricMean[<|a -> 1, b -> 2, c -> 4|>]'
2
```
