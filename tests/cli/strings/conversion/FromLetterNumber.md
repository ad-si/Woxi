# `FromLetterNumber`

Returns the letter at position `n` in the alphabet.

```scrut
$ wo 'FromLetterNumber[5]'
e
```

A second argument selects a named alphabet.

```scrut
$ wo 'FromLetterNumber[3, "Greek"]'
γ
```
