# `StringRiffle`

Joins a list of strings, inserting a separator between each element.

```scrut
$ wo 'StringRiffle[{"a", "b", "c"}]'
a b c
```

```scrut
$ wo 'StringRiffle[{"a", "b", "c"}, ", "]'
a, b, c
```
